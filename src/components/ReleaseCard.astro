---
const { release } = Astro.props;
const youtubeHref = release.youtubeId
  ? `https://www.youtube.com/watch?v=${release.youtubeId}`
  : release.playlistHref || release.links.youtubePlaylists || release.links.youtube;
const youtubeLabel = release.youtubeId ? 'YouTube' : 'YouTube Playlist';
const trackKey = release.title.toLowerCase().replace(/[^a-z0-9]+/g, '_').replace(/^_|_$/g, '');
const detailHref = `/songs/${release.slug}/`;
---

<article class="release-card reveal">
  <a class="release-cover" href={youtubeHref} target="_blank" rel="noopener noreferrer" aria-label={`${release.title} YouTube 또는 재생목록에서 듣기`}>
    <img
      src={release.image}
      alt={`${release.title} album artwork`}
      loading="lazy"
      onerror="this.onerror=null; this.src='/assets/sunofox-neon-icon-512.png'; this.classList.add('release-cover-fallback');"
    />
    <span>PLAY</span>
  </a>

  <div class="release-body">
    <p>{release.tag}</p>
    <h3>{release.title}</h3>
    <span>{release.ko}</span>
    <small>{release.desc}</small>
  </div>

  <div class="release-actions" aria-label={`${release.title} artist and platform links`}>
    <a href={detailHref} data-track={`release_${trackKey}_detail`}>Detail</a>
    <a href={youtubeHref} target="_blank" rel="noopener noreferrer" data-track={`release_${trackKey}_youtube`}>{youtubeLabel}</a>
    <a href={release.links.spotify} target="_blank" rel="noopener noreferrer" data-track={`release_${trackKey}_spotify`}>Spotify Artist</a>
    <a href={release.links.melon} target="_blank" rel="noopener noreferrer" data-track={`release_${trackKey}_melon`}>Melon Artist</a>
    <a href={release.links.kakaoMusic} data-track={`release_${trackKey}_kakaomusic`}>KakaoMusic</a>
    <a href={release.links.appleMusic} target="_blank" rel="noopener noreferrer" data-track={`release_${trackKey}_apple`}>Apple Music</a>
    <a href={release.links.bugs} target="_blank" rel="noopener noreferrer" data-track={`release_${trackKey}_bugs`}>Bugs</a>
  </div>
</article>
