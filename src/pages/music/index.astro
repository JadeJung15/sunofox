---
import SiteChrome from '../../components/SiteChrome.astro';
import Layout from '../../layouts/Layout.astro';
import {
  archiveAlbum,
  musicArchive,
  musicArchiveStructuredData
} from '../../data/siteContent.js';

const visibleVideos = musicArchive.videos.slice(0, 4);
const videoAnchorItems = Array.from(
  new Map(
    visibleVideos.map((video) => [
      video.type,
      {
        label: video.type,
        href: `#music-video-${video.videoId}`
      }
    ])
  ).values()
);
const linkHubCount = musicArchive.linkHub.reduce((total, group) => total + group.links.length, 0);
---

<Layout
  title="SunoFox Music Archive | Anime OST Studio"
  description={musicArchive.summary}
  canonical="https://sunofox.com/music/"
  image={`https://sunofox.com${archiveAlbum.image}`}
  imageAlt={archiveAlbum.imageAlt}
  imageWidth={1600}
  imageHeight={1600}
  jsonLd={musicArchiveStructuredData}
>
  <SiteChrome active="music" />

  <main id="main-content" class="site-main music-archive-page">
    <section class="music-archive-hero music-archive-hero-quiet reveal" aria-labelledby="musicArchiveTitle">
      <div class="music-archive-copy">
        <p class="section-kicker">YOUTUBE / ANIME OST</p>
        <h1 id="musicArchiveTitle"><span>MUSIC</span><span>ARCHIVE</span></h1>
        <p>
          앨범, 주요 스트리밍 채널, YouTube 링크만 간결하게 정리했습니다.
        </p>
        <span>Album · Streaming · YouTube · 확인 기준 {musicArchive.researchDate}</span>
      </div>
    </section>

    <section id="musicLinkHub" class="music-link-hub-section reveal" aria-labelledby="musicLinkHubTitle">
      <div class="novel-section-head">
        <p class="section-kicker">LINK HUB</p>
        <h2 id="musicLinkHubTitle">Music Links</h2>
        <span>{linkHubCount} links</span>
      </div>

      <div class="music-link-grid">
        {musicArchive.linkHub.map((group) => (
          <article class="music-link-card" data-link-group={group.key}>
            <p class="section-kicker">{group.kicker}</p>
            <h3>{group.title}</h3>
            <p>{group.summary}</p>
            <div class="music-link-actions">
              {group.links.map((link) => (
                <a
                  class="main-button main-button-muted"
                  href={link.href}
                  target={link.href.startsWith('http') ? '_blank' : undefined}
                  rel={link.href.startsWith('http') ? 'noopener noreferrer' : undefined}
                >
                  {link.label}
                </a>
              ))}
            </div>
          </article>
        ))}
      </div>
    </section>

    <section class="music-video-section reveal" aria-labelledby="musicVideoTitle">
      <div class="novel-section-head">
        <p class="section-kicker">최근 영상</p>
        <h2 id="musicVideoTitle">Recent Videos</h2>
        <span>{visibleVideos.length} recent</span>
      </div>

      <nav class="music-video-anchor-strip" aria-label="영상 유형 빠른 이동">
        {videoAnchorItems.map((item) => (
          <a href={item.href}>{item.label}</a>
        ))}
      </nav>

      <ol class="music-video-grid">
        {visibleVideos.map((video) => (
          <li
            id={`music-video-${video.videoId}`}
            data-video-id={video.videoId}
            data-video-type={video.type}
            data-video-date={video.date}
          >
            <a href={video.href} target="_blank" rel="noopener noreferrer" data-video-card={video.videoId}>
              <img
                src={video.thumbnail}
                alt={video.thumbnailAlt}
                width="480"
                height="360"
                loading="lazy"
                decoding="async"
              />
              <span>{video.date}</span>
              <strong>{video.title}</strong>
              <p>{video.meta}</p>
              <em>{video.type}</em>
            </a>
          </li>
        ))}
      </ol>
    </section>

    <section class="album-source-panel reveal" aria-label="Music archive sources">
      <span>출처</span>
      {musicArchive.sources.map((source) => (
        <a href={source.href} target="_blank" rel="noopener noreferrer">{source.label}</a>
      ))}
    </section>
  </main>
</Layout>
